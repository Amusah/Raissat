deployment:
  tasks:
    - /bin/rm -rf dist
    - /usr/bin/npm install
    - /usr/bin/npm run build
    - export DEPLOYPATH=/home/raisewqd/public_html
    - /bin/rm -rf $DEPLOYPATH/*
    - /bin/cp -R dist/* $DEPLOYPATH/